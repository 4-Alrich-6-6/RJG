(function () {
  /**
   * Notification storage feed.
   * localStorage value is expected as JSON array of:
   * { id, title, body, createdAt: ISO string, kind?: 'application'|'applicant' }
   */
  // DB-reliant notifications: use window.RJGDb in pages (async).
})();
